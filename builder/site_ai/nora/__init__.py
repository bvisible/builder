# //// Neoffice — added package (no upstream equivalent). What the editor agent gains on a Neoffice
# //// instance: the whole-site playbook in its system prompt, the generate_site tool that runs our
# //// site pipeline on upstream's page engine, and the endpoint the dashboard uses to start a site.
