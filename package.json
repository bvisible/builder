{
    "private": true,
    "workspaces": [
        "frontend",
        "frappe-ui"
    ],
    "type": "module",
    "scripts": {
        "postinstall": "cd frontend && yarn install",
        "dev": "cd frontend && yarn dev",
        "build": "if [ -d 'builder/public/frontend/assets' ] && [ -z \"$FORCE_REBUILD\" ]; then echo '[skip-build] pre-built artifacts present at builder/public/frontend/assets (set FORCE_REBUILD=1 to force rebuild)'; else yarn build:force; fi",
        "build:force": "cd frontend && yarn build"
    },
    "version": "0.0.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {},
    "resolutions": {
        "lint-staged": "^15.2.10"
    }
}
